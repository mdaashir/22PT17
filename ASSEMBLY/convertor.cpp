#include <iostream>
#include <stdlib.h>
#include <string>

using namespace std;

int main(){
/*
    int n;
    char ch = 'y';
    while (ch != 'n' || ch != 'N'){
        cout << "ENTER THE NUMBER: ";
        cin >> n;
        cout << "\t\t\t MENU \n[1] BINARY \n[2] OCTAL \n[3] HEXADECIMAL \nENTER THE CHOICE: ";
        cin >> ch;
        if (ch == 1){
            cout << "BINARY VALUE OF GIVEN NUMBER: "<<bin(n);
        }
        else if (ch == 2){
            cout << "OCTAL VALUE OF GIVEN NUMBER: "<<ocl(n);
        }
        else if (ch == 3){
            cout << "HEXADECIMAL VALUE OF GIVEN NUMBER: "<<HEX(n);
        }
    }
    */
    cout << string();
        return 0;
}
